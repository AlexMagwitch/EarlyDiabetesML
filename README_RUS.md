---

## 🇷🇺 Описание на русском

### Обзор проекта

**EarlyDiabetesML** — учебно-исследовательский проект по предсказанию риска диабета на ранних стадиях на основе анкетных данных (симптомы + простая информация о пациенте).

В репозитории собраны:

- `early_diabetes_ml.py` — основной скрипт с анализом данных и моделями машинного обучения  
- `diabetes_data.csv` — исходный датасет (разделитель `;`)  
- `EarlyDiabetesML_Thesis.pdf` — текст выпускной квалификационной работы (thesis)  
- `presentation.pptx` — презентация к защите работы  
- `requirements.txt` — список Python-зависимостей  
- `describe.csv` — файл с описательной статистикой, который автоматически создаётся скриптом

---

### Используемые модели

В проекте обучаются и сравниваются несколько классических моделей классификации:

- **Логистическая регрессия (Logistic Regression)**
- **Метод k ближайших соседей (k-Nearest Neighbors, kNN)**
- **Решающее дерево (Decision Tree)**
- **Случайный лес (Random Forest)**

Для каждой модели выводятся метрики качества:

- Accuracy (точность классификации)
- Recall (полнота)
- Precision (точность по положительному классу)
- F1-score

Также генерируются визуализации:

- распределение целевого признака (наличие/отсутствие диабета);
- распределения по полу, возрасту и ряду симптомов;
- матрица корреляций признаков (heatmap).

---

### Датасет

- Файл: `diabetes_data.csv`  
- Разделитель: `;` (точка с запятой)  
- Целевой столбец: `class` (наличие/отсутствие диабета)  
- Примеры признаков: `gender`, `age`, `polyuria`, `polydipsia`, `sudden_weight_loss`, `weakness`, `alopecia`, `obesity`, и др.

В скрипте поле `gender` приводится к числовому виду:

```python
df["gender"] = df["gender"].replace(["Male", "Female"], [0, 1])
